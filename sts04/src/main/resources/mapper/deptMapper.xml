<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="dept">
  
  	<select id="selectAll" resultType="com.bit.sts04.model.entity.DeptVo">
  		select * from dept order by deptno
  	</select>
  	<select id="selectOne" parameterType="int" resultType="com.bit.sts04.model.entity.DeptVo" >
  		select * from dept where deptno=#{value}
  	</select>
  	<insert id="insertOne" parameterType="com.bit.sts04.model.entity.DeptVo">
  		insert into dept values (#{deptno},#{dname},#{loc})
  	</insert>
  	<delete id="deleteOne" parameterType="int">
  		delete from dept where deptno=#{value}
  	</delete>
  	<update id="updateOne" parameterType="com.bit.sts04.model.entity.DeptVo">
  		update dept set dname=#{dname},loc=#{loc} where deptno=#{deptno}
  	</update>
  </mapper>